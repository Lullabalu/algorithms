# Persistent Segment Tree

Supports **range sum queries** and **point updates with versioning**.

* Build — `O(n)`
* Update — `O(log n)`
* Query — `O(log n)`

Uses **0-based indexing** and ranges `[l, r)`.
