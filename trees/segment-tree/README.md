# Segment Tree

Lazy propagation segment tree supporting:

* range add
* range sum
* point query
* point update

All operations work in **O(log n)** time.
Uses **0-based indexing** and **half-open ranges [l, r)**.
Memory complexity: **O(n)**.
